export enum menuState {
    MainMenu,
    SinglePlayerGame,
    MPLobby,
    PlayerSelect,
    MPGame,
    GameEnd,
    Rules
}

export enum gameType {
    SinglePlayer,
    MultiPlyaer
}

export enum turn {
    Upgrade,
    Battle
}
